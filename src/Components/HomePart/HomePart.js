import React from 'react';
import './HomePart.css'

const HomePart = () => {
    return (
        <div>
            <section className='header'>
                <div className='text-box'>
                    <h1>First, solve the problem. Then, write the code</h1>
                    <p>When to use iterative development? You should use iterative development only on projects that you want to succeed.</p>
                    <a href="/" className='hero-btn'>Visit us</a>

                </div>
            </section>
        </div>
    );
};

export default HomePart;